# Python packages needed for [ACT005]

# Run the following commands one at a time from your command prompt/terminal:

pip install csv
pip install beautifulsoup4
pip install requests
pip install json
pip install pandas
